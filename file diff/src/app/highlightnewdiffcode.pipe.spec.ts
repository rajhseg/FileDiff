import { HighlightnewdiffcodePipe } from './highlightnewdiffcode.pipe';

describe('HighlightnewdiffcodePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightnewdiffcodePipe();
    expect(pipe).toBeTruthy();
  });
});
