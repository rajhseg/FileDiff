import { HighlightdiffcodePipe } from './highlightdiffcode.pipe';

describe('HighlightdiffcodePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightdiffcodePipe();
    expect(pipe).toBeTruthy();
  });
});
