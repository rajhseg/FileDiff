import { HighlightcodePipe } from './highlightcode.pipe';

describe('HighlightcodePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightcodePipe();
    expect(pipe).toBeTruthy();
  });
});
